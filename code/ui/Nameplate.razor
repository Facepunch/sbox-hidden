@using System;
@using Sandbox;
@using Sandbox.UI;
@using Facepunch.Hidden;
@using Sandbox.UI.Construct;

@namespace Facepunch.Hidden
@inherits Panel

@code
{
	public Label NameLabel;

    public Nameplate( HiddenPlayer player )
	{
		NameLabel = Add.Label( player.Client.Name );
	}
}
